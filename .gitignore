# Dependencies
node_modules/

# Local config with secrets (optional: copy config.js to config.local.js and ignore)
config.local.js
.env
.env.local
*.local.js

# Supabase
.supabase/

# OS
.DS_Store
Thumbs.db

# Windows downloaded file metadata (avoid dirty git status)
*:Zone.Identifier

# Local notes / artifacts (keep out of repo)
implementação.rtf
